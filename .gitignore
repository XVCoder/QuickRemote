# 迁移遗留 / 本地工具数据
.trae/
.codegraph/

# 发布产物 / 大体积二进制
*.apk
*.aab
*.zip
*.tar.gz
*.keystore

# 构建输出 / 反编译参考产物
**/bin/
**/obj/
**/dist/
**/build/
.gradle/
**/publish/
**/publish-selfcontained/
freerdp_apk/

# relay-server 编译产物（早前手动构建遗留，正式产物在 dist/）
QuickRemote/relay-server/quickremote-relay-*

# IDE / 操作系统
.vs/
.idea/
*.user
Thumbs.db
.DS_Store

# 本地配置 / 密钥
config.yaml
local.properties
*.db

# WorkBuddy 本地工作记录（不纳入版本库）
.workbuddy/memory/
.workbuddy/overview.md

# gradle 临时环境（绕过 wrapper 锁 / native dll 加载问题）
.gradle-home*/
.gradle-proj-cache*/
.build-out/
init-redirect.gradle
.tmp*

# PC 端 UI 离屏渲染工作台的产物
tmp-settingshot/*.png*
tmp-settingshot/*.log

# about 页本地调试产物 / 上传编码临时文件 / 平台对照探针
*.b64
.probe-app/
QuickRemote-about/data/
QuickRemote-about/.smoke.log
