refactor(rdp): 彻底移除 RDP/FreeRDP 实现, 统一截屏方案, 发布 v1.1.16/v1.0.20

问题: 内网 RDP 直连再次导致 PC 本地控制台黑屏

Android (v1.0.20):
- 删除 FreeRdpClient/RdpSurfaceView/RdpSessionManager/RdpSessionScreen/LanDiscovery/凭据存储
- 删除 39MB FreeRDP native 库(jniLibs), APK 52MB -> 11MB
- NavGraph 移除内网路由, DeviceListScreen 移除内网直连分区

PC (v1.1.16):
- 删除 RdpConfigurator(注册表/防火墙/NLA 操作)与 LanDiscoveryService(UDP 广播)
- Initialize 不再自动启用 RDP/重配 NLA(黑屏根源)
- MainWindow 移除 RDP 状态卡片/一键启用/紧急恢复 UI
- AppConfig 移除 Rdp 配置节; RelayConnection 端口参数固定 3389(仅兼容服务端注册表)

所有远程连接统一走截屏方案(DXGI 捕获 + H.264/JPEG + 中继隧道)
