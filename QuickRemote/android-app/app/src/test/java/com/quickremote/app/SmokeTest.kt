package com.quickremote.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * 测试脚手架冒烟用例：确认本地 JVM 单测源集可用。
 * 纯逻辑（编解码 / 分片 / 退避策略）以此为基线走 TDD。
 */
class SmokeTest {
    @Test
    fun placeholder() {
        assertEquals(2, 1 + 1)
    }
}
