<Window x:Class="QuickRemote.PCClient.Views.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="更新记录" Height="500" Width="560"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        FontFamily="{StaticResource UiFont}">
    <Border Background="{StaticResource BgSecondaryBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="Transparent"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="46" Margin="20,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="更新记录" FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Click="BtnClose_Click"
                            Style="{StaticResource TitleCloseButton}">
                        <Path Data="M3,3 L9,9 M9,3 L3,9" Width="12" Height="12"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                    </Button>
                </Grid>
            </Border>

            <!-- 内容 -->
            <Grid Grid.Row="1" Margin="20,4,20,20">
                <FlowDocumentScrollViewer x:Name="ChangelogViewer"
                                          Background="{StaticResource BgCardBrush}"
                                          BorderThickness="0"
                                          VerticalScrollBarVisibility="Auto"
                                          IsToolBarVisible="False"
                                          Padding="18,14"
                                          Visibility="Collapsed">
                    <!-- 内部 ScrollViewer 复用主题滚动条样式（6px 窄轨道、圆角、半透明高亮） -->
                    <FlowDocumentScrollViewer.Template>
                        <ControlTemplate TargetType="FlowDocumentScrollViewer">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Style="{StaticResource DarkScrollBar}"
                                              CanContentScroll="True"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                              VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"/>
                            </Border>
                        </ControlTemplate>
                    </FlowDocumentScrollViewer.Template>
                </FlowDocumentScrollViewer>
                <TextBlock x:Name="StatusText"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="12" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Border>
</Window>
