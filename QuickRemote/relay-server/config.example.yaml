server:
  # HTTP/API 端口：仅 Android App 使用（登录认证/设备列表/隧道请求/日志上传）。
  # PC 端不使用此端口，只用控制连接（listen+1）与隧道数据端口。
  # 控制连接端口无需配置，自动为 listen 端口 + 1（如 8443 → 8444）。
  listen: ":8443"
  # 隧道数据端口（PC 与 App 的数据桥接），默认 ":8445"
  tunnel_listen: ":8445"
  tls:
    cert: ""
    key: ""
auth:
  pre_shared_key: "change-me-please"
  jwt_secret: "jwt-signing-secret"
storage:
  sqlite_path: "./registry.db"
quickdeploy:
  base_url: ""
  # 上传令牌：客户端上报的日志会转发到 quickdeploy 的 user_logs 目录。
  # 通过 quickdeploy 的 create_upload_token（target_dir 为 user_logs）生成。
  upload_token: ""
